# CrystalSKK

Windows 向けの SKK 日本語入力メソッド。ここでは SKK の入力操作と変換に関わる語彙を定める。
SKK は日本語の文法解析を行わず、語の区切りを利用者がシフト操作で明示する入力方式であり、
その「区切りを明示する」操作そのものが本プロジェクトの中心的な概念になる。

## Language

### 入力の状態

**直接入力 (Direct)**:
打鍵がそのまま文字として確定していく状態。SKK の既定の状態。
_Avoid_: 通常モード、確定モード

**見出し語入力 (Composing)**:
変換したい語の読みを入力している状態。画面には `▽` が付く。
_Avoid_: 未変換、プリエディット、composition

**候補選択 (Selecting)**:
辞書から得た候補を選んでいる状態。画面には `▼` が付く。
_Avoid_: 変換中、conversion

**辞書登録 (Registration)**:
候補が尽きたときに、新しい語を入力して辞書に加える状態。
この状態の中でさらに変換が行えるため、入れ子になりうる。
_Avoid_: 単語登録、新規登録

**入力モード (InputMode)**:
かな変換を通すか、通した結果をどの字種で出すかの設定。ひらがな・カタカナ・半角カタカナ・全角英数・半角英数の5つ。
上記の「状態」とは直交する概念であり、混同しない。
_Avoid_: モード（単独では曖昧）

### 変換の材料

**見出し語 (Midashi)**:
辞書を引くための読み。利用者がシフト操作で開始を明示した地点から始まる。
_Avoid_: 読み、キー、yomi

**送り仮名 (Okuri)**:
活用語尾。利用者がシフト操作で開始を明示することで、見出し語と区別される。
_Avoid_: 活用語尾、送り

**送りあり / 送りなし**:
変換が送り仮名を伴うかどうかの区別。辞書の見出しは両者で異なる形を取るため、
辞書を引く前に必ず確定している必要がある。
_Avoid_: 活用あり、送り仮名付き

**辞書キー (DictKey)**:
辞書を引くときの文字列。送りなしなら見出し語そのもの、送りありなら見出し語に
送り仮名の最初のローマ字一文字を付けたもの (`おくり` の変換なら `おくr`)。
_Avoid_: 見出し（見出し語と紛らわしい）

**候補 (Candidate)**:
辞書が返す変換結果の一件。注釈を伴うことがある。
_Avoid_: 変換候補、エントリ

**注釈 (Annotation)**:
候補に付随する説明文。辞書上では候補と `;` で区切られている。
_Avoid_: コメント、説明

### 変換の処理

**候補ソース (CandidateSource)**:
辞書キーから候補を生み出すもの。静的辞書・ユーザー辞書・補完・予測など、
出どころの異なるものが並立する。
_Avoid_: 辞書（具体的な一つの辞書を指すときだけ使う）

**ランカー (Ranker)**:
得られた候補の提示順を決めるもの。学習履歴や文脈を根拠にする。
_Avoid_: ソート、並べ替え

**確定 (Commit)**:
未確定の文字列をアプリケーションへ入力し、入力状態を直接入力に戻すこと。
_Avoid_: 決定、挿入

**暗黙の確定**:
確定操作を行わずに次の入力を始めたことで、結果的に確定が起きること。
SKK が打鍵数を減らせる理由の一つ。
_Avoid_: 自動確定

**ローマ字変換規則 (Rule)**:
打鍵列を、出力するかなと次に持ち越す打鍵列の組に対応づけるもの。
持ち越しがあることで促音 (`tt` → `っ` + `t`) を規則として表せる。
_Avoid_: かなテーブル、マッピング
